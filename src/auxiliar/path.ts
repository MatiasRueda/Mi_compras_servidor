export enum PATH {
    // GET ROUTES
    CANJES = "/canjes",
    NOVEDADES = "/novedades",
    SUSCRIPCIONES = "/suscripciones",

    // POST ROUTES
    INGRESAR = "/ingresar",
    REGISTRAR = "/registro",

    // PUT ROUTES
    ACTUALIZAR = "/actualizar",
}