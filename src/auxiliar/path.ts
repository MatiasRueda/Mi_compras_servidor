export enum PATH {
    // GET ROUTES
    USUARIOS = "/usuarios",
    CANJES = "/canjes",
    NOVEDADES = "/novedades",
    SUSCRIPCIONES = "/suscripciones",

    // POST ROUTES
    INGRESAR = "/ingresar",
    REGISTRAR = "/registro",

    // PUT ROUTES
    ACTUALIZAR = "/actualizar",
}